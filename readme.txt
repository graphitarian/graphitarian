# Graphitarian Homepage

Open `index.html` in VS Code / Live Server.

Files:
- index.html — responsive homepage
- style.css — complete styling
- assets/ — local image assets
- fonts/ — embedded local Inter Display fonts

Notes:
- Replace the placeholder contact details in `index.html`.
- Replace the portfolio images in `assets/` with your real work when ready.
- The homepage reference image is included as `assets/homepage-reference.png`.
